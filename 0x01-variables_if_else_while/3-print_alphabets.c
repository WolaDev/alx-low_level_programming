#include <stdio.h>

/**
 * main - Alphabets in lowercase followed by UPPERCASE
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lowercase;
	char uppercase;

	lowercase = 'a';
	uppercase = 'A';

	while (lowercase <= 'z')
	{
		putchar(lowercase);
	}
	while (uppercase <= 'Z')
	{
		putchar(uppercase"\n");

		return (0);
}
